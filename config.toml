title = "Calum Bird"
description = "I'll try to publish helpful reads here. Mostly learnings from my journey in the software & startup universes. Don't take everything I say at face value, there is a high probability that I am just as wrong as anyone else."
countdown_date = "2035-01-01"

# The URL the site will be built for
base_url = "https://calumbird.com"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = false

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = false

generate_feed = false

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true

[extra]
# Put all your custom variables here
github = "https://github.com/calum-bird"
mail = "calum@trelent.net"

favicon = "/imgs/zola-favicon.ico"